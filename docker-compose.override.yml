version: '3.8'

# Development Override Configuration for Docker Compose
services:
  postgres:
    ports:
      - "5432:5432"

  backend:
    environment:
      SPRING_PROFILES_ACTIVE: dev
      LOGGING_LEVEL_COM_CAREEROSAI: DEBUG

  frontend:
    ports:
      - "80:80"
